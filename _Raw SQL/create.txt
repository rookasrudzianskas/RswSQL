CREATE DATABASE test;
SHOW DATABASES;

USE test;

STATUS;

CREATE DATABASE test2 CHARSET utf32;

SHOW ENGINES;

DROP DATABASE test2;

USE test;

CREATE TABLE phones;

CREATE TABLE phones
(
	ID INT UNSIGNED
);

DROP TABLE phones;

CREATE TABLE users
(
	ID INT UNSIGNED UNIQUE,
	Name VARCHAR(20),
	Passwd VARCHAR(16),
	Registration_date TIMESTAMP
);

DROP TABLE users;

CREATE TABLE users
(
	ID INT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
	Name VARCHAR(20),
	Passwd VARCHAR(16),
	Registration_date TIMESTAMP
);

CREATE TABLE users
(
	ID INT UNSIGNED AUTO_INCREMENT,
	Name VARCHAR(20),
	Passwd VARCHAR(16),
	Registration_date TIMESTAMP,
	PRIMARY KEY(ID)
);

DESCRIBE users;

DROP TABLE users;

CREATE TABLE users
(
	ID INT UNSIGNED AUTO_INCREMENT,
	Name VARCHAR(20) NOT NULL DEFAULT 'Unknown',
	Passwd VARCHAR(16),
	Registration_date TIMESTAMP,
	PRIMARY KEY(ID)
);

DROP TABLE users;

CREATE TABLE users
(
	ID INT UNSIGNED AUTO_INCREMENT,
	Name VARCHAR(20) NOT NULL DEFAULT 'Unknown',
	Passwd VARCHAR(16),
	Registration_date TIMESTAMP,
	PRIMARY KEY(ID)
)CHARSET UTF32;

DROP TABLE users;

CREATE TABLE users
(
	ID INT UNSIGNED AUTO_INCREMENT,
	Name VARCHAR(20) NOT NULL DEFAULT 'Unknown',
	Passwd VARCHAR(16),
	Registration_date TIMESTAMP,
	PRIMARY KEY(ID)
)CHARSET UTF32 COLLATE utf32_lithuanian_ci;

REATE TABLE users
(
	ID INT UNSIGNED AUTO_INCREMENT,
	Name VARCHAR(20) NOT NULL DEFAULT 'Unknown',
	Passwd VARCHAR(16),
	Registration_date TIMESTAMP,
	PRIMARY KEY(ID)
)CHARSET utf8mb4 COLLATE utf8mb4_unicode_ci;

CREATE TABLE users
(
	ID INT UNSIGNED AUTO_INCREMENT,
	Name VARCHAR(20) NOT NULL DEFAULT 'Unknown' COLLATE latin1_swedish_ci,
	Passwd VARCHAR(16),
	Registration_date TIMESTAMP,
	PRIMARY KEY(ID)
)CHARSET utf8mb4 COLLATE utf8mb4_unicode_ci;


